{
  "name": "goldprice-tauri",
  "version": "2.2.28",
  "description": "Gold price desktop client (Tauri)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:win10": "tauri build --target x86_64-pc-windows-msvc",
    "build:all": "npm run build:win10",
    "setver": "node scripts/setver.js"
  },
  "author": "Lucas",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0",
    "echarts": "^6.0.0"
  }
}
